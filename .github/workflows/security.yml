name: Security

on:
  workflow_dispatch: # disabled - uncomment push/pr triggers to re-enable
  # push:
  #   branches: [main]
  # pull_request:

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Security Check
        run: echo "Security check coming soon"
