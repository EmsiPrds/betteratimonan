name: CD

on:
  workflow_dispatch: # disabled - uncomment push trigger to re-enable
  # push:
  #   branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: echo "CD coming soon"
